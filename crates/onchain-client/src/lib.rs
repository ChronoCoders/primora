#![deny(warnings)]
#![deny(missing_docs)]
//! Alloy read-only chain access and mint proposal signing.

/// Signs a mint proposal with the backend key and returns it.
pub fn sign_mint_proposal(_proposal: common::MintProposal) -> common::MintProposal {
    todo!()
}
